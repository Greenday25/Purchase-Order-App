<Window x:Class="PurchaseOrderApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PurchaseOrderApp.ViewModels"
        mc:Ignorable="d"
        Title="Purchase Order Dashboard"
        Height="760"
        Width="1320">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <Image Source="Assets/logo.png"
                       Width="110"
                       Height="55"
                       Stretch="Uniform"
                       VerticalAlignment="Center"
                       Margin="0,0,14,0" />
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="PURCHASE ORDER HISTORY" FontSize="24" FontWeight="Bold" />
                    <TextBlock Text="Track approvals, upload signed orders, and store supplier invoices against each order."
                               Foreground="#666666" />
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Refresh History"
                        Width="120"
                        Command="{Binding RefreshHistoryCommand}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1"
                Margin="0,18,0,12"
                Padding="14"
                Background="#F7F7F7"
                BorderBrush="#D6D6D6"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="340" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Search Orders"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                        <Button Grid.Column="1"
                                Content="New Order"
                                Width="120"
                                Margin="10,0,0,0"
                                Click="OnNewOrder" />
                    </Grid>
                    <TextBox Text="{Binding HistorySearchText, UpdateSourceTrigger=PropertyChanged}"
                             Height="30"
                             VerticalContentAlignment="Center" />
                    <TextBlock Text="Search by order number, supplier, status, company, or reference."
                               Margin="0,6,0,0"
                               Foreground="#666666"
                               TextWrapping="Wrap" />
                </StackPanel>

                <TextBlock Grid.Column="1"
                           Text="Double-click an order in the history table to view its workflow, upload the signed order or supplier invoice, or delete it before completion."
                           Foreground="#444444"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Margin="18,0,0,0" />
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Border BorderBrush="#D6D6D6"
                    BorderThickness="1"
                    Padding="8">
                <DataGrid ItemsSource="{Binding FilteredOrderHistory}"
                          SelectedItem="{Binding SelectedOrderHistoryItem}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          RowHeaderWidth="0"
                          MouseDoubleClick="OnHistoryRowDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Order #" Binding="{Binding OrderNumber}" Width="130" />
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="105" />
                        <DataGridTextColumn Header="Company" Binding="{Binding CompanyName}" Width="250" />
                        <DataGridTextColumn Header="Bill To" Binding="{Binding BillTo}" Width="180" />
                        <DataGridTextColumn Header="Reference" Binding="{Binding Reference}" Width="*" />
                        <DataGridTextColumn Header="Status" Binding="{Binding OrderStatus}" Width="150" />
                        <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=N2}" Width="110" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</Window>
